# 02. Locomotion & CameraSystem (로코모션 및 카메라 시스템)

---

## 1. VContainer 주입 관계

```mermaid
classDiagram
    direction TB

    class LocomotionLifetimeScope {
        +Configure(IContainerBuilder builder)
    }

    class CameraFollowService {
        -PureDataCameraSetting m_cameraSetting
        -IMouseWorldPositionProvider m_mousePositionProvider
        -Dictionary m_strategies
        -InputReader m_inputReader
        +CameraFollowService(PureDataCameraSetting, IMouseWorldPositionProvider, IReadOnlyList~ICameraModeCalculationStrategy~, InputReader)
    }

    class LocomotionLogicSystem {
        -PureDataLocomotion _pureData
        -RuntimeDataLocomotion _runtimeData
        -ILocomotionVisualizer _visualizer
        -InputReader _inputReader
        -ILockOnController _lockOnController
        +LocomotionLogicSystem(PureDataLocomotion, RuntimeDataLocomotion, ILocomotionVisualizer, InputReader, ILockOnController)
    }

    class ICameraFollowService {
        <<interface>>
        +SetCameraMode(CameraMode mode)
        +SetRequireRightClickToRotate(bool require)
        +CurrentMode CameraMode
        +CurrentTargetPosition Vector3
        +CurrentLookAngles Vector2
    }

    LocomotionLifetimeScope ..> CameraFollowService : RegisterEntryPoint Singleton (As ICameraFollowService, ILateTickable)
    LocomotionLifetimeScope ..> LocomotionLogicSystem : RegisterEntryPoint Singleton (As ITickable)

    CameraFollowService ..|> ICameraFollowService
```

---

## 2. 데이터 흐름 및 호출 시퀀스

```mermaid
sequenceDiagram
    autonumber
    actor User as 입력 장치
    participant IR as InputReader
    participant ICM as InputContextManager
    participant CFS as CameraFollowService
    participant STRAT as ThirdPersonShoulderCameraStrategy
    participant VIS as CameraFollowVisualizer

    User->>IR: 마우스 이동 입력 (Look Delta)
    IR->>CFS: 마우스 델타 데이터 축적
    Note over CFS: LateUpdate 프레임 도달
    CFS->>ICM: CurrentContext 조회
    alt UI 컨텍스트 활성화 상태
        CFS->>CFS: 회전 각도 연산 무시 (Delta = Vector2.zero)
    else 인게임 플레이 컨텍스트 상태
        CFS->>CFS: 회전 각도 갱신 (LookAngles 계산)
        CFS->>STRAT: CalculateCameraTransform(Target, Angles, Zoom)
        STRAT-->>CFS: 계산된 카메라 위치 및 회전 반환
        CFS->>VIS: 물리 카메라 Transform 적용
        CFS-->>CFS: OnLookAnglesChanged 이벤트 발행
    end
```

---

## 3. 인터페이스 명세 및 Public 메서드 연결점

```mermaid
classDiagram
    class ICameraFollowService {
        <<interface>>
        +Vector3 CurrentTargetPosition
        +Vector2 CurrentLookAngles
        +float CurrentZoomSize
        +CameraMode CurrentMode
        +event Action~Vector3~ OnTargetPositionChanged
        +event Action~Vector2~ OnLookAnglesChanged
        +event Action~float~ OnZoomSizeChanged
        +event Action~CameraMode~ OnCameraModeChanged
        +SetCameraMode(CameraMode mode) void
        +SetRequireRightClickToRotate(bool require) void
    }

    class ILockOnController {
        <<interface>>
        +Transform CurrentTarget
        +bool IsLockOnActive
        +ToggleLockOn() void
        +ClearLockOn() void
    }
```

### 연결 매핑 테이블
| 호출 주체 (Caller) | 공개 메서드 / 프로퍼티 (Public API) | 수신 주체 (Callee) | 전달/반환 데이터 (Payload) | 비고 |
|---|---|---|---|---|
| TacticalCommandLogicSystem | `ICameraFollowService.SetRequireRightClickToRotate(bool)` | CameraFollowService | `bool require` (우클릭 회전 필수 여부) | 시간 정지 모드 시점 회전 제한 |
| UnitCasterSystem | `ICameraFollowService.CurrentMode` | CameraFollowService | 반환 `CameraMode` (현재 카메라 모드) | 시점 모드별 시전 전략 교체 |
| UnitCasterSystem | `ICameraFollowService.OnCameraModeChanged` | CameraFollowService | 이벤트 구독 `Action<CameraMode>` | 숄더뷰/탑뷰 전환 감지 |
| LocomotionLogicSystem | `ILockOnController.IsLockOnActive` | PlayerLockOnController | 반환 `bool` | 전투 락온 중 이동 방향 고정 |

---

## 4. 아키텍처 점검 사항
- `CurrentTargetPosition`과 `TargetPosition`, `CurrentLookAngles`와 `LookAngles`의 프로퍼티 중복 노출을 정리하여 단일화할 필요가 있습니다.
- `CameraFollowService`가 `InputContextManager.Instance` 정적 싱글톤을 직접 참조하고 있으므로, 추후 생성자 주입으로 전환하여 결합도를 낮추는 리팩토링이 권장됩니다.
